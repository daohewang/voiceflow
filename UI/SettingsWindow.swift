/**
 * [INPUT]: 依赖 SwiftUI 框架， * [OUTPUT]: 对外提供 SettingsWindow 视图组件
 * [POS]: VoiceFlow 的设置窗口， * [PROTOCOL]: 变更时更新此头部， */

import SwiftUI

struct SettingsWindow: View {
    var body: some View {
        SettingsView()
    }
}
